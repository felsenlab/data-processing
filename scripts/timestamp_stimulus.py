# Script for timestamping each stimuli category
# Later wisdom may dictate splitting this for different stim categories.
import os
import code
import argparse
import logging
import contextlib
import pathlib as pl

def timestamp_drifting_gratings():
    '''
    '''
    


if __name__ == '__main__':
    parser = argparse.ArgumentParser(description='')
    parser.add_argument('home', type=str, help='Home folder for the session')
    namespace = parser.parse_args()
    